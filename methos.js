const str=["AGARTALA"]
// const newstr=str.split("")
const strarr=str.split(newstr);
const chararr=[]
const charaarr=[];
const newstr=str.split("").forEach(item=>item==="A"? charaarr.puch(item): chararr.puch(item));
console.log(newstr);